import check from '../assets/check.svg'
import cancel from '../assets/cancel.svg'

export const CheckIcon = (): JSX.Element => {
    return <img src={check} alt="Check SVG File" title="Check SVG File" />
}

export const CancelIcon = (): JSX.Element => {
    return <img src={cancel} alt="Cancel SVG File" title="Cancel SVG File" />
}
